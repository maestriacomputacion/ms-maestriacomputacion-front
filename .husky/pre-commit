#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

if ! git diff --cached --name-only -- "src/app/modules/gestion-matricula-academica" | grep -q .; then
  exit 0
fi

if command -v npx >/dev/null 2>&1; then
  npx lint-staged
else
  bunx lint-staged
fi
